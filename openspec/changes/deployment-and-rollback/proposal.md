# Deployment and rollback

Make production deployment a first-class, OCI-based object with environments,
domains, health gates, logs, revisions, and source/configuration rollback.

Depends on runtime, workspace/preview, capabilities, and controllers.

