# Capabilities, providers, bindings, and secrets

Define durable application capabilities, provider selection, framework/generic
bindings, managed/external/adopted modes, and secret isolation.

Depends on application foundation, inspector, and agent protocol.

