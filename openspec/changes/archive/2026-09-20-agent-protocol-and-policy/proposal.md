# Agent protocol and policy

Define the pluggable AgentBackend contract, normalized event stream, platform
tool precedence, permissions, approvals, and native reference-agent boundary.

Depends on `application-foundation-and-desired-state` and `inspector-and-adoption`.

