# Runtime and sandbox

Define language-independent runtime execution with Generic Container as the
compatibility floor, native adapters by support tier, development/production
profiles, and Docker-based sandbox controls.

Depends on the foundation and inspector changes.

