# Design

Use structured events with actor, application, environment, action, resource,
before, after, result, and timestamp metadata. Logs are separated by agent,
build, runtime, deployment, capability, and resource source. Verifier stages
deterministic checks, build/test, health, schema, browser, optional LLM advice,
and human verification; later stages cannot erase earlier failures.

