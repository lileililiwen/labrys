# Design

Inspect Dockerfiles, compose files, manifests, framework conventions,
configuration, CI, and source in that order. Plugins provide language/framework
detectors. The result is an Application Profile containing detected facts and
unknowns with evidence. An Adoption Plan classifies existing items as KEEP,
ADOPT, or OPTIONAL MIGRATION; applying it is explicit and incremental.

