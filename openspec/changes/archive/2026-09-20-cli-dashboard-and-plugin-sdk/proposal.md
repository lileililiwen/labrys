# CLI, dashboard, and plugin SDK

Expose the Application platform through a CLI and dashboard and stabilize the
process-boundary plugin model for six extension families.

Depends on every prior change.

